[user]
    name = Laurent De Marez
    email = laurent@demarez.org

[alias]
    co = checkout
    ll = log --graph --pretty=format':%C(yellow)%h%C(cyan)%d%C(reset) %s %C(white) %C(red bold)%an%C(reset), %C(green)%ar%C(reset)'
    l = !git --no-pager ll -8
    s = status -s

[color]
    ui = auto

[color "status"]
    added = yellow bold
    changed = green bold
    untracked = red bold
